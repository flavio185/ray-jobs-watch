apiVersion: v1
kind: Service
metadata:
  name: {{ include "watch-rayjob-app-chart.fullname" . }}
  labels:
    {{- include "watch-rayjob-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "watch-rayjob-app-chart.selectorLabels" . | nindent 4 }}
